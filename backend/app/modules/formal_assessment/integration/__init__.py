"""Ports onto everything UC-09 does not own: UC-01 … UC-08, the platform's audit pipeline, the
learner-profile source, the assessor directory, the notification channel and the assessor review
queue. Implementations are bound in ``container.py`` and nowhere else.
"""
