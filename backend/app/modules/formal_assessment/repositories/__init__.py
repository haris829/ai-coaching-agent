"""Persistence contracts and the provisional in-memory implementations of them.

**No schema, no ORM, no migration and no SQL.** The company database is bound to the protocols in
``protocols.py`` at integration time; see ``docs/INTEGRATION.md``.
"""
