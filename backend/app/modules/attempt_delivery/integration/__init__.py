"""Boundaries to capabilities UC-03 depends on but does not own.

Each sub-package holds a port (a Protocol describing exactly what UC-03 needs) and a
provisional local adapter used until the real use case is merged. UC-03's services
depend only on the ports.
"""
