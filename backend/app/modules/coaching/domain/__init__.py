"""UC-07's domain: the coaching gate, the sanitisation boundary, the session model and the
review queue. No I/O, no framework, no provider — every rule in here is a pure function or an
immutable value object, which is what makes the security claims testable in isolation.
"""
