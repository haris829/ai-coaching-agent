"""Persistence contracts and the provisional in-memory implementations that satisfy them.

``protocols.py`` is the contract; ``in_memory.py`` is what the coaching tests run against, and
``sqlalchemy.py`` is the implementation over UC-07's own ``qk_`` tables (``coaching/models.py``).
"""
