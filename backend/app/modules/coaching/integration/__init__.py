"""Ports onto everything UC-07 does not own: UC-03, UC-04, UC-06, the AI provider, the
knowledge-gap store and the coaching activity log.

Every one of them is a Protocol. UC-07 implements none of the upstream use cases and never writes
to them (§36).
"""
