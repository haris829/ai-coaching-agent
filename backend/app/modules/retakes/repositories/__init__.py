"""Persistence contracts, and a provisional in-memory binding for them.

``protocols`` is the contract the company database adapter must satisfy. ``in_memory`` is the
default binding so the module runs standalone; it is not a database and is never described as one.
"""
