# UC-04 Lesson Coaching - example configuration. Placeholders only, no real values.
ANSWER_GENERATOR=fake
COURSES_PROVIDER=mock
# Swap to the real Courses Agent with this one value (see docs/INTEGRATION.md section 7):
#   COURSES_PROVIDER=company_courses
#   COMPANY_COURSES_BASE_URL=
#   COMPANY_COURSES_API_KEY=
LEARNER_CONTEXT_PROVIDER=mock
QUIZ_CLASSIFIER=mock
CONCEPT_TAGGER=mock
CURRENT_USER_PROVIDER=header
INTERACTION_LOG_REPOSITORY=memory
FRAMING_REGISTRY=memory

GENERATION_TIMEOUT_MS=10000
GENERATION_TARGET_P95_MS=3000
QUIZ_MATCH_THRESHOLD=0.85
ALLOW_DEV_SESSION_IDS=false

# Only read when ANSWER_GENERATOR is not "fake". Never commit real values.
GENERATION_PROVIDER=
GENERATION_MODEL=
GENERATION_API_KEY=
